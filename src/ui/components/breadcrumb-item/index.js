import FmBreadcrumbItem from '../breadcrumb/src/breadcrumb-item'

/* istanbul ignore next */
FmBreadcrumbItem.install = function(Vue) {
  Vue.component(FmBreadcrumbItem.name, FmBreadcrumbItem)
}

export default FmBreadcrumbItem
