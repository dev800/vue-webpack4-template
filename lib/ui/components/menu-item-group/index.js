import UiMenuItemGroup from '../menu/src/menu-item-group'

/* istanbul ignore next */
UiMenuItemGroup.install = function (Vue) {
  Vue.component(UiMenuItemGroup.name, UiMenuItemGroup)
}

export default UiMenuItemGroup
