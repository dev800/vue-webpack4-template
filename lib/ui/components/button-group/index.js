import FmButtonGroup from '../button/src/button-group'

/* istanbul ignore next */
FmButtonGroup.install = function (Vue) {
  Vue.component(FmButtonGroup.name, FmButtonGroup)
}

export default FmButtonGroup
