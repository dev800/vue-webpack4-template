import UiButtonGroup from '../button/src/button-group'

/* istanbul ignore next */
UiButtonGroup.install = function (Vue) {
  Vue.component(UiButtonGroup.name, UiButtonGroup)
}

export default UiButtonGroup
