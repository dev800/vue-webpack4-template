import UiCollapseItem from '../collapse/src/collapse-item.vue'

/* istanbul ignore next */
UiCollapseItem.install = function (Vue) {
  Vue.component(UiCollapseItem.name, UiCollapseItem)
}

export default UiCollapseItem
